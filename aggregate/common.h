#ifndef COMMON_H
#define COMMON_H

#define WIN32_LEAN_AND_MEAN
#include <windows.h>

#include <algorithm>
#include <atomic>
#include <format>
#include <functional>
#include <print>
#include <ranges>
#include <sstream>
#include <string>
#include <unordered_map>
#include <vector>

using std::atomic;
using std::atomic_bool;
using std::function;
using std::string;
using std::unordered_map;
using std::vector;

#endif  // COMMON_H